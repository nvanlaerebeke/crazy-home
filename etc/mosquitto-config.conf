# Listen on all interfaces inside the container (not just localhost)
listener 1883
protocol mqtt

# DEV ONLY: allow anonymous to get things going; lock down later.
allow_anonymous true

# Optional but good:
persistence true
persistence_location /mosquitto/data/

log_dest stdout